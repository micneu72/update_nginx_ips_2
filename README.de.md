# update_nginx_ips_2

Erstellen Sie die Liste für nginx zulässt

&lt;&lt;&lt;&lt;&lt;&lt;&lt; HEAD

## Start

# `/opt/update_nginx_ips_2.sh /etc/nginx/allowed-domain.list /etc/nginx/allowed-ips-from-domains-active.conf`

## aufruf

`/opt/update_nginx_ips_2.sh /etc/nginx/allowed-domain.list /etc/nginx/allowed-ips-from-domains-active.conf --ipv6-prefix-len 56`

> > > > > > > 990f599 (bessere IPv6 implementierung)
